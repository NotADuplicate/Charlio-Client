/// @description Get milked
if(global.teamNum[other.num] == global.teamNum[num]) {
	burn = 0;
}
else if(spellShield == 0) {
		with(ball_game) {
			node_send(buffer,"eventName","Status","Target",ball_player.num,"Status Num", 32)
		}
		milkNum = other.num
}